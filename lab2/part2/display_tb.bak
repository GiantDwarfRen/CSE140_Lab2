// task which drives five consecutive 7=segment displays
// CSE140L  Lab 2  Part 2 
// $fdisplay performs a return / new line feed; $fwrite does not
task display_tb(input[6:0] seg_j, seg_d,
  seg_e, seg_f, seg_g, seg_h, seg_i
  );
  int h1;
  begin
  h1 = $fopen("output2.txt");
 // segment A (top bar of 7-segment)
      if(seg_j[6]) $fwrite(" _ ");		// day of week 
      else         $fwrite("   ");		//  
      $fwrite("  ");						//  
      if(seg_d[6]) $fwrite(" _ ");		// hour of day
      else         $fwrite("   ");
      $fwrite(" ");
	  if(seg_e[6]) $fwrite(" _ ");		// ones digit of hour
	  else         $fwrite("   ");
	  $fwrite("  ");
	  if(seg_f[6]) $fwrite(" _ ");		// minute 10s digit
	  else         $fwrite("   ");
      $fwrite(" ");
	  if(seg_g[6]) $fwrite(" _ ");	    // minute 1s digit
	  else         $fwrite("   ");
/*	  $fwrite("  ");				        // optional seconds disp
	  if(seg_h[6]) $fwrite(" _ ");		_
	  else         $fwrite("   ");		 
      $fwrite(" ");
	  if(seg_i[6]) $fwrite(" _ ");
	  else         $fwrite("   ");
*/      $fdisplay();					    // line feed
 // segments FGB						// top verticals, center
 	  if(seg_j[1]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_j[0]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_j[5]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite("  ");                     // days-hrs double space
	  if(seg_d[1]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_d[0]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_d[5]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite(" ");
	  if(seg_e[1]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_e[0]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_e[5]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite("  ");	                    // hrs-mins double space
	  if(seg_f[1]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_f[0]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_f[5]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite(" ");
	  if(seg_g[1]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_g[0]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_g[5]) $fwrite("|");
	  else $fwrite(" ");
	  $fdisplay();
  // segments EDC                       // lower sides, bottom     
      if(seg_j[2]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_j[3]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_j[4]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite("  ");
     if(seg_d[2]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_d[3]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_d[4]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite(" ");
      if(seg_e[2]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_e[3]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_e[4]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite("  ");
      if(seg_f[2]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_f[3]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_f[4]) $fwrite("|");
	  else $fwrite(" ");
	  $fwrite(" ");
      if(seg_g[2]) $fwrite("|");
	  else $fwrite(" ");
	  if(seg_g[3]) $fwrite("_");
	  else $fwrite(" ");
	  if(seg_g[4]) $fwrite("|");
	  else $fwrite(" ");
	  $fdisplay();
	end
endtask